# MCP Server — 环境变量配置
# 复制为 .env 后修改

# 工作目录（容器内路径，对应主机 /home/ubuntu/repo）
WORKSPACE_DIR=/workspace

# 命令超时（秒）
COMMAND_TIMEOUT=120

# 允许读取的文件扩展名（逗号分隔），留空 = 全部允许
# ALLOWED_READ_EXTENSIONS=.py,.txt,.md,.json,.yaml,.yml,.html,.css,.js,.ts,.sh,.toml,.cfg
